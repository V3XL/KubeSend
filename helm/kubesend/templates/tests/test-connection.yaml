apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kubesend.fullname" . }}-test-connection"
  labels:
    {{- include "kubesend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kubesend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
